from setuptools import setup, find_packages

setup(
    name = 'src',
    version = '0.0.1',
    description = 'Wine quality package',
    author = 'Nabeel',
    packages = find_packages(),
    license = 'MIT'
)